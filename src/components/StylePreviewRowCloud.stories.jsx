import React from 'react';
import { COMPONENT } from './StylePreviewRowCloud';

export default {
  title: 'Components/StylePreviewRowCloud',
  component: StylePreviewRowCloud,
};

export const Default = () => <StylePreviewRowCloud />;
